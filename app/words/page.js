import React from "react";
import SingleWord from "@/components/word/SingleWord";

export const metadata = {
  title: "Hachiko Dictionary | Words"
}

const Words = () => {
  return <SingleWord />;
};

export default Words;
